Model artifacts belong here.

This directory is intentionally excluded from the public repository. Use it for local outputs such as:
- trained autoencoder weights
- latent embeddings
- clustering outputs
- anomaly-scoring artifacts

Regenerate these artifacts locally with the research scripts after installing `research/requirements-research.txt`.
