Processed research outputs belong here.

This directory is intentionally excluded from the public repository. It is the local workspace for:
- canonical research records
- feature matrices
- validation summaries
- exported analysis tables

Rebuild these files locally by running the staged pipeline in `research/scripts/`.
Only the small app-facing export in `src/data/research/vulnerability-regimes.json` is kept in the public repo.
